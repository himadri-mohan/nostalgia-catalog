export function AffiliateDisclosure() {
  return (
    <p>
      Affiliate disclosure: some outbound links may include an affiliate parameter when the site
      operator has configured one. Amazon links may add <code>tag</code>, Apple TV links may add{" "}
      <code>at</code>, and Disney+ links may add <code>cid</code>. A purchase through those links
      may earn a commission at no extra cost to you. Library links are not affiliate links. When no
      affiliate id is set, the links stay plain official URLs.
    </p>
  );
}
